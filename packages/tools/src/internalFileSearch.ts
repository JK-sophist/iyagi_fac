export async function internalFileSearch(topic: string): Promise<string[]> {
  return [`Internal memo on ${topic}`, 'Past campaign conversion benchmark: 2.4%', 'Top objections: trust, setup cost'];
}
